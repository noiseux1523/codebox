function[out]=ios(in,history)
% this is ordering with the last success direction.
% I can't really use this one but, I could always use data from
% complete_history to compute another similar direction
end
